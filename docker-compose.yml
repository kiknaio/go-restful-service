version: '3.1'
services:
  mysql-server:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    ports:
    - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: toor
      MYSQL_DATABASE: goDB
      MYSQL_USER: go
      MYSQL_PASSWORD: toor